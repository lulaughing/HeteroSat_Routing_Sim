1. 平均端到端时延 (Average End-to-End Delay)
理论预期图景：Dijkstra 低开高走（指数爆炸），H-IGA 平稳上升。

Dijkstra (静态/盲目)：

低负载阶段：全场最低。因为网络空闲，无排队时延，Dijkstra 总是选择物理距离最近的路径（最短跳数），所以时延表现最好。

高负载阶段：全场最高（指数级飙升）。随着热点路径被填满，根据 M/M/1 或 BPR 模型，排队时延会急剧增加。Dijkstra 不知变通，死磕拥塞路径，导致时延失控。

SGA (标准遗传)：

全阶段：介于两者之间。它能避开部分拥塞，但由于全局搜索效率低，往往会找到一些“虽然通畅但绕路很远”的路径，导致物理传播时延较高。

H-IGA (分层智能)：

低负载阶段：略高于 Dijkstra。因为算法计算开销或为了负载均衡（Load Balancing）可能会预先选择一些稍远的非热点路径。

高负载阶段：全场最低（平稳）。当最短路堵死时，H-IGA 主动切换到 MEO/GEO 或侧向链路。虽然物理距离增加了（绕路），但避免了可怕的排队时延。总时延 = 物理时延(微增) + 排队时延(极低)。

关键特征：在负载约为 30~50 附近会出现一个 “交叉点 (Crossover Point)”，H-IGA 的时延曲线向下穿过 Dijkstra。

2. 平均丢包率 (Average Packet Loss Rate)
理论预期图景：Dijkstra 迅速饱和（高位横盘），H-IGA 长期压制（低位运行）。

Dijkstra：

趋势：极速上升。一旦负载超过最短路径的物理带宽（例如 5Mbps 瓶颈），多余的数据包会被直接丢弃。

峰值：由于缺乏绕路机制，丢包率会迅速稳定在一个很高的数值（例如 30%~40%），这意味着大约 1/3 的数据包因为死守拥塞路径而被丢弃。

SGA：

趋势：缓步上升。比 Dijkstra 好，但受限于收敛速度，可能无法在有限时间内找到所有可用的空闲路径，导致部分业务依然受损。

H-IGA：

趋势：缓慢上升且保持低位。通过适应度函数的强力引导，H-IGA 会像水流一样填满网络的每一个角落（包括非最优路径）。只有当全网总容量都被打满时，丢包率才会显著上升。

数值：在 600 负载下，理论上应保持在 5%~15% 左右（远低于 Dijkstra）。

3. 有效吞吐量 (Avg Goodput)
理论预期图景：Dijkstra 触顶早（拥塞崩塌），H-IGA 持续增长（接近网络容量极限）。

Dijkstra：

趋势：在低负载时随业务量线性增长，但很快触及**“单路径容量上限”。之后即使业务量再增加，Goodput 不再增长，甚至因为严重的丢包重传导致Goodput 下降（拥塞崩塌现象）**。

SGA：

趋势：优于 Dijkstra，但低于 H-IGA。因为它能利用多条路径，但利用得不充分。

H-IGA：

趋势：持续线性增长。H-IGA 能够榨干网络的最后一滴带宽资源（以空间换容量）。它的 Goodput 曲线斜率最接近理想值（Ideal Throughput），直到触及全网物理总容量的瓶颈。